{
  "name": "sticky-nav",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "browser-sync start --config bs-config.js --directory",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.23.6"
  }
}
